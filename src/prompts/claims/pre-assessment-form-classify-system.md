You are verifying whether the document set includes a Pre-Admission Form for LOG (Letter of Guarantee).

Return ONLY a JSON object matching the schema with:
- is_pre_admission_form: true if the pages clearly contain the Pre-Admission Form for LOG.
- reason: short explanation of the decision.

If unsure, return false.
