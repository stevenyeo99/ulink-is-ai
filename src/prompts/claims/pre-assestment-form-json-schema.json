{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "patient_name": {
      "type": "string",
      "description": "Part (1) patient info table: Patient name (handwritten). Copy exactly as written."
    },
    "policy_no": {
      "type": "string",
      "description": "Part (1) patient info table: Policy number / Member number. If the field is blank or not present, return empty string."
    },
    "nrc_or_passport": {
      "type": "string",
      "description": "Part (1) patient info table: NRC / Passport number. Copy exactly as written. If redacted/masked/blackened/blurred or unreadable, return empty string."
    },
    "date_of_birth": {
      "type": "string",
      "description": "Part (1) patient info table: Date of birth. Copy exactly as written (do not normalize format)."
    },
    "admission_or_appointment_date": {
      "type": "string",
      "description": "Part (1) patient info table: Admission/Appointment date (the row where the sample shows 19.11.2024). Copy exactly as written."
    },
    "hospital_name": {
      "type": "string",
      "description": "Part (1) patient info table: The row after date fields that records hospital/clinic/provider (label is Burmese). Copy exactly what is written in that row. Do NOT take values from adjacent rows (doctor name, Ward/Township). If text looks like job status (e.g., 'On duty'), it belongs to doctor name, not hospital_name. If blank/unclear, return empty string."
    },
    "doctor_name": {
      "type": "string",
      "description": "Part (1) patient info table: This mapped field corresponds to the duty/occupation style row under provider/hospital in this form variant. Copy exactly what is written in that row (example: 'On duty'). Do NOT copy from hospital/clinic/provider row (example: 'Ar Yu'). If blank, return empty string."
    },
    "ward_or_township": {
      "type": "string",
      "description": "Part (1) patient info table: Ward / Township / locality (row label includes 'Ward' and 'Township'). Copy exactly as written. If blank, return empty string."
    },
    "email": {
      "type": "string",
      "description": "Part (1) contact section: Email address. Copy exactly (keep punctuation, @, dots). If blank, return empty string."
    },
    "phone_number": {
      "type": "string",
      "description": "Part (1) contact section: Phone number. Copy exactly as written (do not reformat). If blank, return empty string."
    },
    "diagnosis": { "type": "string", "description": "Diagnosis / Provisional Diagnosis." },
    "extra_fields": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "label": { "type": "string" },
          "value": { "type": "string" }
        },
        "required": ["label", "value"]
      }
    }
  },
  "required": [
    "patient_name",
    "policy_no",
    "nrc_or_passport",
    "date_of_birth",
    "admission_or_appointment_date",
    "hospital_name",
    "doctor_name",
    "ward_or_township",
    "email",
    "phone_number",
    "diagnosis",
    "extra_fields"
  ]
}
